# E-Reader 1.0.3

Windows 正式版，面向本地 EPUB 与无 DRM 的 KF8/AZW3 阅读，新增整本书全文搜索。

## 新增：整本书全文搜索

- 阅读时按 `Ctrl+F` 搜索当前书籍的全部章节，支持 EPUB 与无 DRM 的 KF8/AZW3。
- 支持完整句子和多个关键词，自动忽略换行、多余空格、中英文标点、大小写及全半角差异。
- 显示所有匹配结果、章节名称与正文上下文，点击后准确跳转并临时高亮。
- 按 `Esc` 关闭搜索，不改变现有分页、阅读位置、目录、全屏和字体体验。
- 搜索索引仅在本机内存中按需生成，不联网、不引入账号、云同步或额外数据库。
- 已通过 142 个章节、约 243 万字符的真实长篇 EPUB，以及 19 个章节真实 AZW3 的完整验证。

## 延续的稳定性修复

- 翻页位置改为使用 Chromium 实际计算出的栏宽与栏间距，消除小数像素误差在连续翻页后的累积漂移。
- 修复阅读到较后页面时最右侧中文偶发显示不完整的问题。
- 为分页正文加入左右对称的字形安全留白，避免最右侧中文、衬线字形及选中背景被分页边界裁切。
- 保持单页宽度与翻页步长一致，不改变左右边距对称性。

## 功能

- 本地书架、搜索、排序、阅读进度与原书封面缩略图。
- 单屏单页分页阅读、目录、键盘与页面边缘翻页、字号与主题设置。
- E-Ink 风格日间/夜间渲染，英文使用 Lora，中文使用 Noto Serif SC，字体完全离线提供。
- 已移除朗读、Python 运行时、语音模型和音频缓存，安装体积明显降低。
- 书籍、数据库、设置与日志保存在 `%LOCALAPPDATA%\EReader`，覆盖安装和默认卸载均保留。

## 限制

- 仅支持 Windows x64。
- AZW3 仅支持无 DRM 的 KF8 内容；不提供任何解密能力。
- 本开发发行版未做代码签名，Windows 可能显示未知发布者警告。
- 不包含在线更新、云同步、商店或遥测。
